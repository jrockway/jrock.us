namespace: prometheus
resources:
    - tailscale.yaml
    - tailscale.sealed-secret.json
configMapGenerator:
    - name: prom-rules
      files:
          - prometheus.yml
          - legacy.yml
      options:
          disableNameSuffixHash: true
