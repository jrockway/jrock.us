apiVersion: apps/v1
kind: Deployment
metadata:
    name: cert-manager-cainjector
    namespace: cert-manager
spec:
    template:
        spec:
            containers:
                - name: cert-manager
                  resources:
                      requests:
                          cpu: 10m
                          memory: 128Mi
                      limits:
                          memory: 128Mi
